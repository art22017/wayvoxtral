# WayVoxtral keyd configuration
# Install to: /etc/keyd/wayvoxtral.conf
# Then run: sudo keyd reload

[ids]
# Apply to all keyboards
*

[main]
# Intercept Ctrl+Space globally and convert to F24
# F24 is a rarely used key that WayVoxtral listens for
control+space = macro(F24)
